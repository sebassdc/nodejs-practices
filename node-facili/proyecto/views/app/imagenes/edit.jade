extends ../../layout.jade
block contenido
    div(class="col-md-6 no-float center-block big-top-space")
        h1 Editar imagen
        form(action="/app/imagenes/#{imagen._id}?_method=PUT", method="POST")
            div(class="form-group")
                label(for="title") Título
                input(value="#{imagen.title}" class="form-control", type="text", name="title", placeholder="Titulo", id="title")
            div(class="row top-space")
                div(class="col-xs-12 col-md-6")
                    input(type="submit" value="Guardar" class="btn btn-info")
